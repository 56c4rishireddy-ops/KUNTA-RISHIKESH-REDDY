#include <stdio.h>
int main()
{
	int arr[10], max, min, size, i, sum = 0;
	float avg;
	
printf("enter size of the array (<10): ");
scanf("%d", &size);

printf("enter elements of the array :");

for(i = 0; i< size; i++)
   {
   	scan
   }
}